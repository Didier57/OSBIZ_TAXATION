<Window x:Class="OsbizTaxation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="OSBIZ TAXATION" Height="640" Width="860"
        WindowStartupLocation="CenterScreen"
        MinHeight="520" MinWidth="720">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="OSBIZ TAXATION" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Version, StringFormat={}  v{0}}" FontSize="12"
                       VerticalAlignment="Bottom" Margin="6,0,0,3" Foreground="Gray" />
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Configuration" Padding="8" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="URL source (HTTPS)" VerticalAlignment="Center" Margin="0,4" />
                <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="4"
                         Padding="4" MinHeight="26"
                         VerticalContentAlignment="Center"
                         Text="{Binding SourceUrl, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Fichier texte de sortie" VerticalAlignment="Center" Margin="0,4" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="4"
                         Padding="4" MinHeight="26"
                         VerticalContentAlignment="Center"
                         Text="{Binding OutputFile, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="1" Grid.Column="2" Content="Parcourir..."
                        Padding="10,2" Margin="4"
                        Command="{Binding BrowseOutputFileCommand}" />

                <CheckBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="4,8,4,4"
                          Content="Activer la recuperation automatique"
                          IsChecked="{Binding AutoFetchEnabled}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Intervalle (minutes)" VerticalAlignment="Center" Margin="0,4" />
                <TextBox Grid.Row="3" Grid.Column="1" Margin="4" Width="80" HorizontalAlignment="Left"
                         Padding="4" MinHeight="26"
                         VerticalContentAlignment="Center"
                         Text="{Binding AutoFetchIntervalMinutes, UpdateSourceTrigger=PropertyChanged}" />

                <CheckBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Margin="4,8,4,4"
                          Content="Verifier les mises a jour au demarrage"
                          IsChecked="{Binding CheckUpdatesOnStartup}" />
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Recuperer maintenant" Padding="14,6" Margin="0,0,8,0"
                    Command="{Binding FetchNowCommand}" />
            <Button Content="Enregistrer la configuration" Padding="14,6" Margin="0,0,8,0"
                    Command="{Binding SaveConfigCommand}" />
            <Button Content="Verifier les mises a jour" Padding="14,6" Margin="0,0,8,0"
                    Command="{Binding CheckUpdatesCommand}" />
            <Button Content="Ouvrir le dossier" Padding="14,6" Margin="0,0,8,0"
                    Command="{Binding OpenOutputFolderCommand}" />
            <Button Content="Effacer le journal" Padding="14,6"
                    Command="{Binding ClearLogCommand}" />
        </StackPanel>

        <Border Grid.Row="3" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="0,4" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Status, StringFormat={}Statut : {0}}" FontWeight="SemiBold" Margin="0,0,20,0" />
                <TextBlock Text="{Binding LastFetch, StringFormat={}Derniere recuperation : {0}}" Foreground="Gray" />
            </StackPanel>
        </Border>

        <GroupBox Grid.Row="4" Header="Journal" Padding="4">
            <ListBox ItemsSource="{Binding Logs}"
                     FontFamily="Consolas" FontSize="12"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" />
        </GroupBox>
    </Grid>
</Window>
