<Window x:Class="OsbizTaxation.Views.ImportExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Import / Export" Height="620" Width="820"
        WindowStartupLocation="CenterOwner"
        MinHeight="480" MinWidth="640">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Base de donn&#233;es" Padding="8">
            <StackPanel>
                <TextBlock x:Name="TxtCount" Text="0 appel(s) en base." Margin="0,0,0,8" />
                <StackPanel Orientation="Horizontal">
                    <Button Content="Supprimer les doublons" Padding="14,4" Margin="0,0,8,0" Click="OnSupprimerDoublonsClick" />
                    <Button Content="Supprimer toutes les donn&#233;es" Padding="14,4" Click="OnSupprimerToutClick" />
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Importer des fichiers raw" Padding="8" Margin="0,10,0,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Site des fichiers" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox x:Name="CmdSite" Width="260" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Importer un fichier..." Padding="14,4" Margin="0,0,8,0" Click="OnImporterFichierClick" />
                    <Button Content="Importer un dossier..." Padding="14,4" Click="OnImporterDossierClick" />
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Journal" Margin="0,10,0,0">
            <TextBox x:Name="TxtJournal"
                     IsReadOnly="True"
                     IsUndoEnabled="False"
                     FontFamily="Consolas" FontSize="12"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     BorderThickness="0"
                     Background="Transparent"
                     HorizontalScrollBarVisibility="Auto"
                     VerticalScrollBarVisibility="Auto" />
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Fermer" Padding="18,6" Click="OnFermerClick" />
        </StackPanel>
    </Grid>
</Window>
